while(c <= fim ){
    var c = i 
    var i = 2
    var inicio = Number(i.value)
    var f = 10
    var fim = Number(f.value)
    var p = 2
    var passo = Number(p.value)
    c+= i
    console.log( inicio)
    }