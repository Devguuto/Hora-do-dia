const form = window.document.getElementById ('form-deposit') // criamos uma constante ao nosso formulario usando o getelementebyid

function validanome (nomecompleto){
    nomecompleto.split(\)
}


form.addEventListener('submit', function(e) { //adicionamos um evento ao "submit" do formulario e demos o nome do parâmetro dessa função de "e"
    e.preventDefault();                       // o preventDefault, retira a função submit 
})
